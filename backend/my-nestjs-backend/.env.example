# Backend Configuration - Copy thành .env và điền giá trị

# ============= DATABASE =============
DATABASE_URL=postgresql://user:password@localhost:5432/btnhom

# ============= EMAIL - SENDGRID =============
# Lấy từ https://sendgrid.com/
# 1. Đăng ký tài khoản
# 2. Vào Settings > API Keys > Create API Key
# 3. Copy API Key
SENDGRID_API_KEY=SG.xxxxx_your_api_key_xxxxx

# Email gửi từ (phải được verify trong SendGrid)
# Ví dụ: noreply@btnhom.com hoặc your-email@gmail.com
SENDGRID_FROM_EMAIL=noreply@btnhom.com

# ============= FRONTEND URL =============
# Dùng để tạo link xác thực email trong email template
# Verification link: {FRONTEND_URL}/verify-email?token=xxx
FRONTEND_URL=http://localhost:5173

# ============= JWT (Optional - cho bước tiếp theo) =============
# JWT_SECRET=your_secret_key_here_very_long_and_random
# JWT_EXPIRATION=24h
